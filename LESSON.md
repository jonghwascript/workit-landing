# 타원형 구현 
```
.hero-curve {
  clip-path: ellipse(70% 30% at 45% 51%);
}
```

# 패딩에 배경색을 제외 
```
background-clip: content-box;
```
# padding-bottom에만 배경색을 제외할려면
HTML 구조를 건드리지 않고, 배경 역할을 하는 가상 요소를 만들어 아래쪽 공간만 비워두는 방식입니다. 반응형에서도 레이아웃이 매우 안정적입니다.
```
.section {
  position: relative;
  padding: 40px 20px 80px 20px; /* 아래쪽 패딩 80px */
  z-index: 1;
}

.section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 100px;
  /* 아래쪽 패딩 크기만큼 띄움 -> 배경색 제외 효과 */
  background-color: $solate-color-100;
  /* 원하는 배경색 */
  z-index: -1;
  /* 콘텐츠 뒤로 보내기 */
}
```
# 반응형을 위한 이미지 변경사항
```
  width: 100%;             /* 부모 공간에 맞춰 유연하게 늘어남 */
  max-width: 320px;        /* 단, 320px 이상으로는 커지지 않음 */
  height: auto;            /* 가로 너비에 맞춰 세로를 자동으로 줄임 */
  aspect-ratio: 320 / 184; /* 원본 비율대로 미리 공간을 잡아둠 *
```
## 가변 길이를 이용해 넓이 구하기 
target % content = reulst 
```
 151width %  48px = 3.14em;
```

# CSS에서는 콤마(,)로 구분하여 하나의 요소에 배경 이미지를 원하는 만큼 여러 개 넣을 수 있습니다.


# 부드럽고 유동적인 위치 지정 
현재 작성하신 코드에서 `left -38%`와 같은 **퍼센트(%)** 단위도 이미 유동적인 단위입니다.

하지만 화면을 크기를 조절해 보면 이미지가 화면 밖으로 훅 날아가 버리거나, 예상치 못한 곳으로 휙휙 이동하는 느낌을 받으실 겁니다. 이는 퍼센트가 '부모 요소의 너비'에 비례하여 곱해지기 때문에, 화면이 넓어질수록(768px -> 1024px 등) `-38%`의 실제 픽셀 값이 기하급수적으로 커져서 이미지를 밖으로 강하게 밀어내기 때문입니다.

이러한 현상을 잡고 원하는 대로 부드럽게 유동적인 위치를 잡으려면 다음 두 가지 실무 방식을 사용합니다.

### 1. `vw` (뷰포트 단위) 사용하기 (가장 직관적)

부모 박스 크기가 아니라 브라우저 화면 너비(`vw`)를 기준으로 이동시킵니다. 화면이 커지는 비율과 정확히 일치하게 이미지가 부드럽게 이동합니다.

```scss
    /* 위치 지정 */
    background-position:
      /* 퍼센트 대신 화면 너비(vw) 단위 사용 */
      left -10vw top 28%,
      right -5vw top 81%;

```

### 2. `calc()` 함수로 최소/최대 한계점 만들기 (가장 디테일함)

화면이 아무리 커져도 이미지가 일정 수준 이상으로는 도망가지(밀려나지) 않게 닻(Anchor)을 내리면서 유동성을 줍니다. **고정 픽셀(px)과 유동 단위(vw 또는 %)를 섞어 쓰는 방식**입니다.

```scss
    /* 위치 지정 */
    background-position:
      /* 왼쪽 끝에 걸쳐두고(0px), 화면이 커질 때 5vw 정도만 살짝 이동하게 함 */
      left calc(-100px + 5vw) top 28%, 
      
      /* 오른쪽 끝(0%)에 고정하고, 고정 픽셀만큼만 밖으로 빼기 */
      right -50px top 81%; 

```

**💡 실무 적용 팁**
현재 1번 이미지가 왼쪽 밖으로 많이 삐져나가게 연출하고 싶으신 거라면, `%` 단위보다는 `left -150px`처럼 고정 픽셀로 위치를 단단히 잡아두는 것이 태블릿~데스크톱 구간에서 레이아웃이 덜컹거리는 것을 막는 가장 안전한 방법입니다.

한번 `%`를 `px`이나 `vw`로 바꿔서 브라우저 창을 늘렸다 줄였다 테스트해 보세요! 훨씬 안정적으로 움직일 것입니다.

# transform: translate에서 사용된 %는 뷰포트가 아닌 요소 자체의 너비와 높이를 기준으로 작동하므로, 뷰포트(화면 크기)에 따라 이동하게 하려면 % 대신 vw와 vh 단위를 사용해야 합니다.

`transform: translate`에서 사용된 `%`는 뷰포트가 아닌 **요소 자체의 너비와 높이**를 기준으로 작동하므로, 뷰포트(화면 크기)에 따라 이동하게 하려면 `%` 대신 **`vw`와 `vh` 단위**를 사용해야 합니다.

```css
transform: translate(-7vw, -17vh);

```

* **`vw` (Viewport Width)**: 화면 전체 너비의 1%를 의미합니다. (`-7vw` = 화면 너비의 7%만큼 왼쪽으로 이동)
* **`vh` (Viewport Height)**: 화면 전체 높이의 1%를 의미합니다. (`-17vh` = 화면 높이의 17%만큼 위로 이동)

만약 요소 자체의 중심점 이동(`%`)과 뷰포트 기준의 이동(`vw`, `vh`)을 동시에 조합해야 하는 상황이라면, 아래와 같이 `calc()` 함수를 활용할 수 있습니다.

```css
/* 예: 요소 크기의 50%만큼 이동한 뒤, 추가로 뷰포트 기준 7vw, 17vh만큼 이동 */
transform: translate(calc(-50% - 7vw), calc(-50% - 17vh));

```

174~177라인은 한꺼번에 외우기보다, `::before`로 장식용 도형을 만드는 예제로 공부하면 좋습니다.

```scss
inset: 0 0 45%;
border-radius: 0 0 50% 50% / 0 0 32px 32px;
background-color: $solate-color-900;
pointer-events: none;
```

각 줄을 풀면 다음과 같습니다.

### 174라인: `inset`

```scss
inset: 0 0 45%;
```

`position: absolute` 요소의 위치를 지정하는 축약 속성입니다.

값이 3개이면 다음처럼 해석합니다.

```scss
inset: top right bottom;
```

따라서 실제 의미는:

```scss
top: 0;
right: 0;
bottom: 45%;
left: 0;
```

가상 요소가 부모의 위·왼쪽·오른쪽에는 붙고, 아래에서는 부모 높이의 `45%`만큼 떨어집니다.

공부할 때는 먼저 4개 값을 모두 적어보세요.

```scss
inset: 0 0 45% 0;
```

### 175라인: `border-radius` 고급 문법

```scss
border-radius: 0 0 50% 50% / 0 0 32px 32px;
```

`/` 앞은 모서리의 가로 반지름이고, `/` 뒤는 세로 반지름입니다.

```text
가로 반지름 / 세로 반지름
```

네 값의 모서리 순서는 다음과 같습니다.

```text
왼쪽 위 → 오른쪽 위 → 오른쪽 아래 → 왼쪽 아래
```

따라서 위쪽 모서리는 각지게 유지하고, 아래쪽 두 모서리만 둥글게 만듭니다. 그 결과 아래쪽이 활처럼 휘어진 배경이 만들어집니다.

이 속성은 직접 값을 바꿔보는 것이 가장 좋습니다.

```scss
border-radius: 0;
border-radius: 0 0 50% 50%;
border-radius: 0 0 50% 50% / 0 0 32px 32px;
border-radius: 0 0 50% 50% / 0 0 100px 100px;
```

특히 마지막의 `32px`을 키우면서 곡선이 얼마나 깊어지는지 확인해 보세요.

### 176라인: 배경색

```scss
background-color: $solate-color-900;
```

가상 요소의 배경색을 SCSS 변수에 저장된 진한 보라색으로 지정합니다.

```scss
$solate-color-900: #24053e;
```

가상 요소는 내용이 없기 때문에 `inset`으로 크기를 만들고, 이 속성으로 실제 색을 보이게 합니다.

### 177라인: 클릭 방해 방지

```scss
pointer-events: none;
```

가상 요소가 마우스 클릭이나 터치의 대상이 되지 않게 합니다. 장식용 요소가 버튼이나 링크 위를 덮더라도 사용자 입력을 가로채지 않습니다.

공부 순서는 이렇게 잡으면 됩니다.

1. `position: relative`와 `position: absolute`
2. `top/right/bottom/left`
3. 그것의 축약형인 `inset`
4. `border-radius` 기본 문법
5. `/`를 사용하는 타원형 `border-radius`
6. `::before`와 `content`
7. `z-index`, `isolation`, `pointer-events`

이 코드를 이해하는 핵심 문장은 다음입니다.

> 부모를 기준으로 가상 요소의 크기를 만든 다음, 아래 모서리를 곡선으로 만들고, 배경색을 칠한 장식 요소가 클릭을 방해하지 않게 한다.


이런 CSS를 처음부터 외워서 작성한다고 생각할 필요는 없습니다. 완성하려는 모양을 **작은 문제로 나누어 생각하는 방식**이 핵심입니다.

이번 코드는 다음 흐름으로 나온 것입니다.

1. “이미지 뒤에 보라색 배경이 필요하다.”
2. “HTML 요소를 추가하지 않고 장식만 만들고 싶다.”
   → `::before`
3. “장식을 `.hero-art` 기준으로 배치해야 한다.”
   → 부모는 `position: relative`, 장식은 `position: absolute`
4. “위와 양옆은 부모에 붙이고, 높이는 조금 줄인다.”
   → `inset: 0 0 45%`
5. “장식 아래쪽만 휘어야 한다.”
   → 아래 모서리에만 `border-radius`
6. “이미지보다 뒤에 있어야 한다.”
   → `z-index: -1`, `isolation: isolate`
7. “장식이 클릭을 막으면 안 된다.”
   → `pointer-events: none`

즉, 처음부터 아래 코드를 통째로 떠올리는 게 아닙니다.

```scss
&::before {
  content: '';
  position: absolute;
  inset: 0 0 45%;
  border-radius: 0 0 50% 50% / 0 0 32px 32px;
  background-color: $solate-color-900;
  pointer-events: none;
  z-index: -1;
}
```

먼저 가장 단순한 사각형부터 만듭니다.

```scss
.hero-art {
  position: relative;

  &::before {
    content: '';
    position: absolute;
    inset: 0;
    background: purple;
  }
}
```

그다음 하나씩 수정합니다.

```scss
// 높이를 줄인다
inset: 0 0 45%;

// 아래를 둥글게 만든다
border-radius: 0 0 50% 50%;

// 곡선의 깊이를 조절한다
border-radius: 0 0 50% 50% / 0 0 32px 32px;

// 콘텐츠 뒤로 보낸다
z-index: -1;
```

앞으로 디자인을 볼 때는 다음 네 가지 질문을 먼저 해보세요.

- 이 모양은 실제 콘텐츠인가, 장식인가?
- 어떤 요소를 기준으로 배치해야 하는가?
- 사각형에서 무엇을 바꾸면 이 모양이 되는가?
- 앞에 있어야 하는가, 뒤에 있어야 하는가?

가장 좋은 연습 방법은 개발자 도구에서 속성을 하나씩 껐다 켜는 것입니다. 특히 `inset`, `border-radius`, `z-index`를 각각 비활성화하면 각 코드가 왜 필요한지 바로 보입니다.

CSS는 “정답 코드를 떠올리는 능력”보다 **사각형을 만들고 조금씩 원하는 모양으로 변형하는 능력**에 가깝습니다.
